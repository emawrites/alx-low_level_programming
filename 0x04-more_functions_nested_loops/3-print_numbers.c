#include "main.h"

/**
 * print_numbers - prints nos from 0 - 9
 *
 * Return: 0.
 */

void print_numbers(void)
{
	int i;

	i = 0;
while (i < 10);

	{
		if (i != 2 && i != 4)
		{
			_putchar(i + '0');
		}
		i++;
	}

	_putchar ('\n')
}
